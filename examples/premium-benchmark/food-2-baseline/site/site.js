(function(){
  var toggle=document.querySelector('.menu-toggle'),nav=document.querySelector('.mobile-nav');
  if(toggle&&nav){toggle.addEventListener('click',function(){var open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));nav.hidden=open});nav.addEventListener('click',function(e){if(e.target.closest('a')){nav.hidden=true;toggle.setAttribute('aria-expanded','false')}})}
  function decorate(){document.querySelectorAll('.chapter-card').forEach(function(card,i){if(card.dataset.ready)return;card.dataset.ready='true';var label=card.querySelector('.chapter-number');if(label)label.textContent=i===0?'Capítulo cítrico':'Capítulo cereal';var name=(card.querySelector('[data-product-field="name"]')||{}).textContent||'';if(/granola/i.test(name)){var desc=card.querySelector('.product-description');if(desc){var note=document.createElement('p');note.className='allergen';note.innerHTML='<strong>Importante:</strong> contiene almendras.';desc.after(note)}}var add=card.querySelector('[data-product-add]'),feedback=card.querySelector('.add-feedback');if(add&&feedback)add.addEventListener('click',function(){add.classList.add('is-added');add.textContent='Añadido';feedback.textContent='Producto añadido a tu pedido.';window.setTimeout(function(){add.classList.remove('is-added');add.textContent='Añadir al pedido'},1300)})})}
  document.addEventListener('pagosya:ready',decorate);if(document.readyState!=='loading')decorate();else document.addEventListener('DOMContentLoaded',decorate);
})();